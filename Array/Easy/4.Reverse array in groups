1.Two Pointer Approach

class Solution {
public:
    void reverseInGroups(vector<int> &arr, int k) {
        int n = arr.size();

        for (int i = 0; i < n; i += k) {
            int st = i;
            int end = min(i + k - 1, n - 1);

            while (st < end) {
                swap(arr[st], arr[end]);
                st++;
                end--;
            }
        }
    }

    
};

2.Using std::reverse()

class Solution {
public:
    void reverseInGroups(vector<int> &arr, int k) {
        int n = arr.size();

        for (int i = 0; i < n; i += k) {
            int end = min(i + k, n);
            reverse(arr.begin() + i, arr.begin() + end);
        }
    }
};

Method	Time	Space	Recommendation
std::reverse()	O(n)	O(1)	⭐ Best/cleanest
Two pointers	O(n)	O(1)	⭐ Best for DSA


-------------------------------------------------------------------------------------------------------------------------------
wrong:

class Solution {
	public:
	void reverseInGroups(vector<int> &arr, int k) {
	    
	    int n=arr.size();
	    int d=k%n;
	    
	    for(int i=0;i<n;i+=d){
	        
	        int a=min(i+d,n);
	        reverse(arr.begin()+i,arr.begin()+a);
	    }
	    
	}
};
int d = k % n;

You should not use k % n.

For this problem, if k > n, the entire array is reversed as one group (depending on the exact problem specification). 
More importantly, if k == n, your code gets:

d = k % n = 0;

Then:

i += d;

becomes:

i += 0;

and your loop becomes infinite. so dont use that .....




